---
title: "工作流"
summary: "工作流的配置、节点类型和使用方法"
tags: ["HiAgent", "workflow"]
type: documentation
source: HiAgent 官方文档
updated: 2026-04-06
---

⼯作流 - HiAgent Document
hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow
HiAgent 中的⼯作流是⼀系列有序的任务和操作，⽤于实现特定⽬标或完成特定业务流程。它可以包括多个步骤，每个步骤有特定任务和操作，可根据需要进⾏分⽀、循环和条件判断等。⼯作流的⽬的是提⾼⼯作效率、减少错误和重复劳动，同时提⾼业务流程的可控性和可管理性。⼯作流⽀持通过可视化的⽅式，对插件、⼤语⾔模型、代码块等功能进⾏组合，从⽽实现复杂、稳定的业务流程编排。当⽬标任务场景包含较多的步骤，且对输出结果的准确性、格式有严格要求时，适合配置⼯作流来实现。实际应⽤中，⼯作流可⽤于各种场景，如订单处理、客户服务、项⽬管理等⽀持⼯作流添加⼤语⾔模型、代码、知识库、选择器、插件、⼯作流等节点，拓展其⼯作流的流程⽀持对⼯作流的发布，试运⾏，展示运⾏结果等⽀持对⼯作流的导⼊、导出、编辑、删除等管理操作1. 登录HiAgent2. 进⼊⽬标⼯作空间，选择⼯作流，点击创建⼯作流3. 设置⼯作流的名称与描述，并单击确认4. 创建完成后，可以在⼯作流管理的⻚⾯列表中，看到该⼯作流⼯作流⼯作流概述创建⼯作流2026/1/30 22:06
⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

2/76

配置项说明⼯作流名称必填⼯作流名称进⾏命名，⽤于标识该⼯作流，清晰准确的名称有助于⽤户检索和选择使⽤该⼯作流由中⽂、字⺟、数字、连接符（-、_）组成不能以连接符（-、_）开头或结尾字符限制：【2，128】字符不可与已有⼯作流名称重复⼯作流描述必填⼯作流描述，让⼤模型理解什么情况下应该调⽤此⼯作流字符限制：【1，500】字符⼯作流logo必填默认系统⾃带的logo图⽚，⽀持刷新后随机获取新的logo可⾃定义上传⼯作流的logo图⽚，⽀持图⽚格式包含：png、jpeg、jpg、bmp2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

3/76

图⽚最⼤⽀持2M上传的图⽚需进⾏裁剪在⼯作流列表中单击⼯作流名称或右侧的编辑按钮，进⼊⼯作流的编辑⻚⾯。⼯作流由多个节点构成，节点是组成⼯作流的基本单元，包含⼤模型、代码、知识库、选择器、插件、⼯作流等技能节点配置使⽤。 ⼯作流默认包含了 Start 和 End 节点。节点与节点之间可以通过链接线连接并关联，形成从开始到结束的流程，⼀个节点可以连接并关联 10 个分⽀的下游节点，最⼤⽀持 10 个分⽀下游节点。⼯作流的起始节点，⽤于设定启动⼯作流需要的信息配置⼯作流Start 节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

4/76

配置项说明输⼊定义启动⼯作流需要的输⼊参数，这些内容将在智能体对话过程中被LLM(⼤语⾔模型)读取，使得LLM可以在合适的时候启动⼯作流并填⼊正确的信息可添加多个⼯作流中需要输⼊的参数变量名必填定义输⼊的变量名由字⺟、数字、连接符（-、_）组成字⺟和下划线开头字符限制：【1，128】字符不可与已有变量名名称重复变量类型必填参数的数据类型可选项包含：String（默认类型）、Integer、Number、Object、Boolean、File、Array、Array、Array、Array2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

5/76

、Array、Array - file类型⽀持明细如下： - image类（image） - png、jpg、jpeg、gif、svg、webp - ⽂档类(doc) - pdf、doc、docx、ppt、pptx、ofd、csv、xls、xlsx - ⽂本⽂件（txt） - html、txt、md、json - ⾳频(voice) - wav、ogg、mp3、aac、m4a - 视频（video） - mp4、avi、mov、flv、wmv、rmvb、webm、mpga - 压缩包(zip) - rar、zip、7z、gz、gzip> 👉 - 复杂参数的参数层级⾄多⽀持设置到5层描述必填变量名的描述字符限制：【1，128】字符必填参数是否必填。打开开关表示当前参数为必填参数，关闭开关表示当前参数为选填参数添加⼦项当数据类型选择 Object 类型时，该配置项会显示加号（+）图标。单击加号图标可在当前 Object 参数内添加⼦参数只⽀持两级嵌套⽀持操作删除：删除变量名链接：链接其他节点，仅可以Start节点开头，链接到下⼀节点⼯作流的最终节点，⽤于返回⼯作流的运⾏后的结果信息End 节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

6/76

配置项说明回复模式必填下拉框，可选项包含： - 使⽤设定的内容直接回答 - 返回变量，由智能体⽣成回答输⼊若添加，则参数名、参数值必填；若未添加，则⾮必填定义结束⼯作流需要的输⼊参数，这些内容将在智能体对话过程中被LLM(⼤语⾔模型)读取，使得LLM可以在合适的时候结束⼯作流并填⼊正确的信息可添加多个⼯作流中需要输⼊的参数参数名若添加输⼊参数，则必填；若未添加，则⾮必填定义输⼊的参数名由字⺟、数字、连接符（-、_）组成字⺟和下划线开头2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

7/76

字符限制：【1，128】字符不可与已有参数名名称重复参数值若添加输⼊参数，则必填；若未添加，则⾮必填下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值回答内容回答⽅式选择使⽤设定的内容直接回答时，有该输⼊框 - 在此模式下，⽀持流式开关，如果输出内容中引⽤变量是来⾃LLM类节点，则开启流式后此部分⽀持流式输出。 > 👉 流式输出约束：1.回复模式是“使⽤设置的内容直接回答”2.回答内容⾥要引⽤变量，且变量引⽤的是LLM节点类的输出参数，包括LLM节点（要求LLM节点的输出模式是⾮JSON类型）、agent节点、⼯作流节点（end节点是流式⽣效）可输⼊回答的内容，当命中参数时，按填写的内容进⾏答复⼤模型节点可以调⽤⼤型语⾔模型，根据输⼊参数和提示词⽣成回复，通常⽤于执⾏⽂本⽣成任务，例如⽂案制作、⽂本总结、⽂章扩写等。 也可以调⽤⽀持图像理解、视频理解的模型，处理图像和视频识别任务。 ⼤模型节点依赖⼤语⾔模型的语⾔理解和⽣成能⼒，可以处理复杂的⾃然语⾔处理任务，你可以根据业务场景的需求选择不同的模型，并配置提示词来定义模型的⼈设和回复⻛格。为了更精准地控制模型⽣成的结果，你还可以在⼤模型节点中设置模型的参数，从⽽影响模型回复的⽂本⻓度、内容的多样性等。⼤模型2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

8/76

配置项说明⼤模型节点名称必填默认⼤模型xx序列号作为命名，⽀持重命名模型下拉选项，可选择HiAgent⽀持的⼤语⾔模型⽀持按模型⾼级能⼒进⾏筛选： - Function call - 图像理解 - 视频理解 - 深度推理模型参数预设模版HiAgent预置以下三种模型参数模版，提供快捷的模型参数配置。创意模版平衡模版精确模版随机性Temperature采样温度，控制输出的随机性，值越⼤会使输出更随机，更具创造性；值越⼩，输出会更加稳定或确定。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

9/76

默认0.7可修改范围：【0，1】之间，⼩位数⼀位核采样top_p控制输出的多样性,值越⼤输出包括更多单词选项；值越⼩，输出内容更集中在⾼概率单词上，即输出更确定但缺少多样性。⼀般temperature和top_p只设置⼀个。单次回复max_tokens单次输出内容最⼤token数。当前时间开启后，⽤户的每次 query 中都会拼上当前准确时间。深度思考模式仅针对⽀持深度思考且⽀持深度思考开关的模型⽣效，不可⼿动配置。⻓思考过程展示仅针对⽀持深度思考的模型⽣效，按需配置。当深度思考模式选择"关闭"时，不显示"⻓思考过程展示"选项。输⼊参数参数名定义⼤语⾔模型需要的输⼊参数，这些内容将在智能体对话过程中被LLM(⼤语⾔模型)读取，使得LLM可以在合适的时候使⽤该参数并填⼊正确的信息若添加输⼊参数，则必填；若未添加，则⾮必填定义输⼊的参数名，输⼊的变量可以被下⽅的提示词调⽤由字⺟、数字、连接符（-、_）组成字⺟和下划线开头字符限制：【1，128】字符不可与已有参数名名称重复2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

10/76

参数值若添加输⼊参数，则必填；若未添加，则⾮必填下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 -输⼊：⽀持设定⾃定义的参数值视觉理解输⼊仅选择⽀持图像理解的模型时可配置。⽤于视觉理解的输⼊，传⼊图⽚url，并可在提示词引⽤。⽀持增加输⼊变量，可添加多个，也可以不添加。 - 参数名只能包含数字、字⺟、中划线或下划线，⻓度1-64。 -参数值仅⽀持引⽤上游节点的img类型/array类型变量，即⽀持单图/多图引⽤。视频理解输⼊仅选择⽀持视频理解的模型时可配置。⽤于视频理解的输⼊，传⼊视频url，并可在提示词引⽤。⽀持增加输⼊变量，可添加多个，也可以不添加。 - 参数名只能包含数字、字⺟、中划线或下划线，⻓度1-64。 -参数值仅⽀持引⽤上游节点的video类型/array2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

11/76

系统提示词模型的系统提示词，⽤于指定⼈设和回复⻛格。⽀持直接引⽤提示词模版、插⼊团队空间已创建的提示词，也可以⾃⾏编写提示词；编写系统提示词时，可以引⽤输⼊参数中的变量。默认使⽤Normal模式，单击下拉框⽀持切换Jinja模版。 >👉 切换后可能会丢失提示词中的变量，请谨慎操作。⽤户提示词模型的⽤户提示词是⽤户在本轮对话中的输⼊，⽤于给模型下达最新的指令或问题。⽤户提示词同样可以引⽤输⼊参数中的变量。默认使⽤Normal模式，单击下拉框⽀持切换Jinja模版。 >👉 切换后可能会丢失提示词中的变量，请谨慎操作。输出值输出变量默认是raw_output可配置输出变量的格式类型：JSON（默认）、⽂本、Markdown、⾃定义 - 输出格式为JSON/⾃定义时，⽀持新增变量，变量类型可灵活定义 > 👉 默认输出变量raw_output输出格式为JSON时，不可修改变量类型；输出格式为⾃定义时，变量类型可选String、Integer、Number。 - 输出格式为⽂本/Markdown时，不⽀持新增变量，变量类型不可修改，默认String超时设置（秒）通过配置超时设置，可以避免⻓时间等待，提⾼系统的响应速度。默认120，取值范围1～600，单位为秒。重试次数重试次数设置允许在请求失败时⾃动重试，提⾼请求的成功率。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

12/76

默认0，取值范围0～3，0表示不重试。异常忽略仅输出格式是json时⽀持配置异常忽略。开启后，⽀持配置异常忽略模板，发⽣异常后，⼯作流运⾏不会中断。开发者可⾃定义设置需要返回的输出字段内容，必须是输出中已定义的字段，且格式为合法的 JSON格式。另外，节点还会返回输出参数 errorBody，传递节点异常的详细信息。若未开启，则节点执⾏异常后会触发⼯作流的终⽌运⾏。⽀持操作复制：复制当前⼤模型及表单内容删除：删除⼤语⾔模型节点单节点调试⽀持对⼤模型单个节点进⾏节点调试编写代码，处理输⼊变量来⽣成返回值。代码2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

13/76

配置项说明代码节点名称必填默认代码xx序列号作为命名，⽀持重命名输⼊参数若添加，则参数名、参数值必填；若未添加，则⾮必填这⾥输⼊的变量可以被下⽅的提示词调⽤定义代码需要的输⼊参数，这些内容将在智能体对话过程中被LLM(⼤语⾔模型)读取，使得LLM可以在合适的时候使⽤该参数并填⼊正确的信息可添加多个代码中需要输⼊的参数参数名若添加输⼊参数，则必填；若未添加，则⾮必填定义输⼊的参数名由字⺟、数字、连接符（-、_）组成2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

14/76

字⺟和下划线开头字符限制：【1，128】字符不可与已有参数名名称重复参数值若添加输⼊参数，则必填；若未添加，则⾮必填下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值代码可以直接使⽤上⽅输⼊参数中的变量，并通过return⼀个变量来输出处理结果可选择代码语⾔：Javascript、Python⽀持代码全屏阅览，可按需调整代码内容在IDE中编辑点击后，左侧抽屉弹窗弹出IDE编辑器，可编辑测试代码输出值代码运⾏完成后输出的变量，此处的变量类型需与代码中的return对象⼀致可添加多个输出值可选项包含：String（默认类型）、Integer、Number、Object、Boolean、Array、Array、Array、Array、Array变量名必填定义输⼊的变量名由字⺟、数字、连接符（-、_）组成字⺟和下划线开头字符限制：【1，128】字符2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

15/76

不可与已有变量名名称重复变量类型必填参数的数据类型可选项包含：String（默认类型）、Integer、Number、Object、Boolean、Array、Array、Array、Array、Array复杂参数的参数层级⾄多⽀持设置到5层添加⼦项当数据类型选择 Object 类型时，该配置项会显示加号（+）图标。单击加号图标可在当前 Object 参数内添加⼦参数> 👉 - 复杂参数的参数层级⾄多⽀持设置到5层超时设置（秒）通过配置超时设置，可以避免⻓时间等待，提⾼系统的响应速度。默认120，取值范围1～180，单位为秒。重试次数重试次数设置允许在请求失败时⾃动重试，提⾼请求的成功率。默认为0，取值范围0～3，0表示不重试。异常忽略开启后，⽀持配置异常忽略模板，发⽣异常后，⼯作流运⾏不会中断。开发者可⾃定义设置需要返回的输出字段内容，必须是输出中已定义的字段，且格式为合法的 JSON 格式。另外，节点还会返回输出参数errorBody，传递节点异常的详细信息。若未开启，则节点执⾏异常后会触发⼯作流的终⽌运⾏。⽀持操作复制：复制当前代码及表单内容删除：删除代码节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

16/76

单节点调试在代码配置项最右侧单击电脑图标，可弹出 IDE 编辑器弹窗，可编辑和测试代码。⽀持选择 Javascript、Python 两种开发语⾔编辑代码。⽀持单击测试代码，填写输⼊值测试并运⾏输出内容，对输出⽆错误的代码⽀持复制和同步更新输出值到 Schema。⽀持对代码单个节点进⾏节点调试。在选定的知识库中，根据设置的召回分段数数量和要求的相似度设置值来召回最匹配的分段信息，并以 List 形式返回。⽀持对知识库单个节点进⾏节点调试。知识库检索2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

17/76

配置项说明知识库检索节点名称必填默认知识库检索xx序列号作为命名，⽀持重命名输⼊参数必填输⼊需要从知识库中匹配的关键信息参数名默认Query如果关联的标准类知识库有配置标签schema，则会按顺序展示标签列表如果关联的API类知识库有配置⾃定义（除Query,TopK,Score外）⼊参，则会按顺序展示⼊参列表参数值必填2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

18/76

下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值知识库选择需要匹配的数据库，并单击编辑参数⽀持⽤户⾃定义过滤条件可选择本空间内已创建的知识库编辑参数设置各标签的过滤条件和组装逻辑，⽤于在检索时根据标签对数据进⾏过滤。标准类知识库：如果有设置标签，则⽀持配置标签值，对于⼿动设置的标签，将不再经由⼤模型⽣成过滤条件，未填写的标签将由模型⾃动⽣成。⼿动输⼊的标签将和⾃动⽣成的标签按照与逻辑形成最终的标签过滤条件。 允许⽤户⾃定义过滤条件，或选择由知识库的过滤模型⾃动⽣成。相关运算符说明： - Integer/Number类型标签： ⽀持`=` `≠` `>` `<``≥` `≤` `为空` `不为空`逻辑条件，默认为`=` - String类型标签: ⽀持`是` `不是` `包含` `不包含` `开始是` `结束是` `为空` `不为空`逻辑条件，默认为`是`。 - Boolean类型标签: ⽀持`是` `不是` `为空` `不为空`逻辑条件，默认为`是`。其中，不是的逻辑包含取反和空值，例如：不是false，则包含true以及空值。API类知识库：如果有⾮系统（Query,TopK, Score）⼊参，则⽀持配置这些⼊参值。检索⽅式向量检索：将数据转换为数学向量，然后在预构建的向量库中找到与查询向量最相似的结果。此种⽅式⽀持对⾮结构化数据的语义匹配。全⽂检索：针对⽂本内容进⾏关键字匹配，并根据关键词的相关性和权重来筛选和排序包含这些词的⽂档。混合检索：融合向量检索与全⽂检索技术，对数据同时进⾏向量化处理和关键词匹配，将两种检索⽅式的结果综合评估与排序。此⽅式既考虑⾮结构化数据的语义相似性⼜兼顾⽂本内容的关键字相关性。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

19/76

Rerank模型仅选择检索⽅式为混合检索时，可配置。⽤于对搜索结果进⾏重新排序的模型。它可以根据⽤户的查询和搜索结果的相关性，对搜索结果进⾏重新排序，以提供更符合⽤户需求的结果。Rerank 打分：重排序模型将根据候选⽂档列表与⽤户问题语义匹配度进⾏重新排序，从⽽改进语义排序的结果。默认⾃动选择Rerank模型。按权重打分：通过调整分配的权重，重新排序策略确定是优先进⾏语义匹配还是关键字匹配。清空模型选项则按权重打分，显示⽆（按权重计算）。权重设置Rerank 模型清空选项则按权重打分时显示该配置项。配置向量和全⽂的计算权重，粒度0.1，默认向量0.7:全⽂0.3。召回分段数智能体在调⽤知识库匹配⽤户输⼊内容时，返回的数据⽚段数量，数值越⼤返回的内容越多相似度设置仅选择检索⽅式为【全⽂检索】时，⽆此配置根据该设置选择知识库内容返回，数值越⼤，相似度越⾼智能体在调⽤知识库匹配⽤户输⼊内容时，会将达到匹配要求的数据⽚段进⾏召回，如果⽚段未达到匹配度，则不会被召回输出值输出是所有选定知识库的召回分段以及元数据信息⽀持操作复制：复制当前知识库及表单内容删除：删除知识库节点知识库写⼊2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

20/76

写⼊节点仅⽀持能按照通⽤分段⽅法的⽂件上传写⼊知识库。节点⽀持以下操作：复制：复制当前节点及表单内容。删除：删除节点。调试：配置输⼊输出参数进⾏对话体验，运⾏结束后，单击展示结果查看运⾏结果。配置项是否必填说明知识库写⼊节点名称是默认知识库写⼊xx序列号作为命名，⽀持重命名。输⼊参数是需要写⼊知识库的⽂档，必须满⾜⽂件类型。注意：请提前在Start节点中输⼊File类型的变量，作为写⼊的⽂档。当前仅⽀持写⼊单个⽂档，批量写⼊需要结合循环节点或批处理节点。知识库是选择已有知识库或新创建知识库，所有上传的⽂档将存⼊选择知识库中。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

21/76

知识库写⼊设置⽂档解析类型是默认通⽤，⽀持上传 DOC、DOCX、HTML、HTM、TXT、PDF、PNG、JPG、JPEG、PPT、PPTX 格式⽂件。分段⽅式是配置将完整⽂件分割成分段的规则，⽀持以下两种分段⽅式：⾃动分割：⾃动设置分段规则与预处理规则。⾃定义分割：⾃定义分段规则、分段⻓度以及预处理规则等。 选择⾃定义分割需要填写分段标识和分段⻓度。分段标识是按照⾃定义分段标识将完整⽂本分割成多个分段，选择⾃定义分割时必填。可选择以下三种分段标识：换⾏：对PDF不⽣效，PDF仍会按版⾯切分。双换⾏：对PDF不⽣效，PDF仍会按版⾯切分。⾃定义：⾃定义分段标识将被视为明⽂⽂字处理。勾选按版⾯⾃动切分，会⾃动识别出标题、正⽂段落、图⽚和表格元素，再将其内容就近合并成不超过设置⻓度的分段。使⽤此⽅法后将忽略分段标识。分段⻓度是每个分段的⽂字⻓度上限，单位为字符数，选择⾃定义分割时必填。⽀持⻓度范围：200 - 15000 整数。有效期是截⽌到有效期将被禁⽤，不再被召回。可选以下两种有效期：永久有效临时有效：从⽂件上传时开始，有效期1个⽉。 示例：⽤户 2025.04.07 00:00:00 上传⽂件，有效期2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

22/76

截⽌时间是 2025.05.07 23:59:59。标签配置否如果知识库启⽤标签过滤，将从⽤户对话中提取标签，并与此处指定的⽂件标签进⾏匹配，只有满⾜过滤条件的⽂件分段会被召回。输出值是输出是写⼊知识库的⽂档信息。在选定的问答库中，根据设置的召回分段数数量和要求的相似度设置值来召回最匹配的问答对，并以 List 形式返回。配置项说明问答库节点名称必填默认问答库xx序列号作为命名，⽀持重命名输⼊参数必填问答库2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

23/76

输⼊需要匹配的关键信息参数名默认Query参数值必填下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值问答库选择需要匹配的库，并单击编辑参数⽀持⽤户⾃定义过滤条件可选择本空间内已创建的问答库编辑参数设置各标签的过滤条件和组装逻辑，⽤于在检索时根据标签对数据进⾏过滤。如果创建问答库时有设置标签，则⽀持配置标签值，对于⼿动设置的标签，将不再经由⼤模型⽣成过滤条件，未填写的标签将由模型⾃动⽣成。⼿动输⼊的标签将和⾃动⽣成的标签按照与逻辑形成最终的标签过滤条件。允许⽤户⾃定义过滤条件，或选择由问答库的过滤模型⾃动⽣成。相关运算符说明： - Integer/Number类型标签： ⽀持`=` `≠` `>` `<``≥` `≤` `为空` `不为空`逻辑条件，默认为`=` - String类型标签: ⽀持`是` `不是` `包含` `不包含` `开始是` `结束是` `为空` `不为空`逻辑条件，默认为`是`。 - Boolean类型标签: ⽀持`是` `不是` `为空` `不为空`逻辑条件，默认为`是`。其中，不是的逻辑包含取反和空值，例如：不是false，则包含true以及空值。检索⽅式向量检索：将数据转换为数学向量，然后在预构建的向量库中找到与查询向量最相似的结果。此种⽅式⽀持对⾮结构化数据的语义匹配。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

24/76

全⽂检索：针对⽂本内容进⾏关键字匹配，并根据关键词的相关性和权重来筛选和排序包含这些词的⽂档。混合检索：融合向量检索与全⽂检索技术，对数据同时进⾏向量化处理和关键词匹配，将两种检索⽅式的结果综合评估与排序。此⽅式既考虑⾮结构化数据的语义相似性⼜兼顾⽂本内容的关键字相关性。Rerank模型仅选择检索⽅式为混合检索时，可配置。⽤于对搜索结果进⾏重新排序的模型。它可以根据⽤户的查询和搜索结果的相关性，对搜索结果进⾏重新排序，以提供更符合⽤户需求的结果。Rerank 打分：重排序模型将根据候选⽂档列表与⽤户问题语义匹配度进⾏重新排序，从⽽改进语义排序的结果。默认⾃动选择Rerank模型。按权重打分：通过调整分配的权重，重新排序策略确定是优先进⾏语义匹配还是关键字匹配。清空模型选项则按权重打分，显示⽆（按权重计算）。权重设置Rerank 模型清空选项则按权重打分时显示该配置项。配置向量和全⽂的计算权重，粒度0.1，默认向量0.7:全⽂0.3。召回分段数返回的数据⽚段数量，数值越⼤返回的内容越多。默认为1。相似度设置仅选择检索⽅式为【全⽂检索】时，⽆此配置如果⽚段未达到匹配度，则不会被召回输出值输出是所有选定库中召回的信息与输⼊参数最匹配的信息2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

25/76

⽀持操作复制：复制当前节点删除：删除节点在选定的术语库中，根据设置的召回分段数数量和要求的相似度设置值来召回最匹配的分段信息，并以 List 形式返回。配置项说明术语库节点名称必填默认术语库xx序列号作为命名，⽀持重命名术语库2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

26/76

输⼊参数必填输⼊需要从术语库中匹配的关键信息参数名默认Query参数值必填下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值术语库选择需要匹配的术语库，并设置相关的参数可选择本空间内已创建的术语库检索⽅式向量检索：将数据转换为数学向量，然后在预构建的向量库中找到与查询向量最相似的结果。此种⽅式⽀持对⾮结构化数据的语义匹配。全⽂检索：针对⽂本内容进⾏关键字匹配，并根据关键词的相关性和权重来筛选和排序包含这些词的⽂档。混合检索：融合向量检索与全⽂检索技术，对数据同时进⾏向量化处理和关键词匹配，将两种检索⽅式的结果综合评估与排序。此⽅式既考虑⾮结构化数据的语义相似性⼜兼顾⽂本内容的关键字相关性。Rerank模型仅选择检索⽅式为混合检索时，可配置。⽤于对搜索结果进⾏重新排序的模型。它可以根据⽤户的查询和搜索结果的相关性，对搜索结果进⾏重新排序，以提供更符合⽤户需求的结果。Rerank 打分：重排序模型将根据候选⽂档列表与⽤户问题语义匹配度进⾏重新排序，从⽽改进语义排序的结果。默认⾃动选择Rerank模型。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

27/76

按权重打分：通过调整分配的权重，重新排序策略确定是优先进⾏语义匹配还是关键字匹配。清空模型选项则按权重打分，显示⽆（按权重计算）。权重设置Rerank 模型清空选项则按权重打分时显示该配置项。配置向量和全⽂的计算权重，粒度0.1，默认向量0.7:全⽂0.3。召回分段数智能体在调⽤术语库匹配⽤户输⼊内容时，返回的数据⽚段数量，数值越⼤返回的内容越多相似度设置仅选择检索⽅式为【全⽂检索】时，⽆此配置根据该设置选择术语库内容返回，数值越⼤，相似度越⾼智能体在调⽤术语库匹配⽤户输⼊内容时，会将达到匹配要求的数据⽚段进⾏召回，如果⽚段未达到匹配度，则不会被召回输出值输出是所有选定术语库中召回的信息与输⼊参数最匹配的信息⽀持操作复制：复制当前术语库及表单内容删除：删除术语库节点⻓期记忆节点⽤于在⼯作流中召回⻓期记忆中储存的⽤户的个性化信息。 节点说明 在⽤户喜好推荐等个性化的场景中，通常需要基于⽤户画像、关键记忆点等个⼈数据进⾏推荐、筛选，让智能体效果更加贴合⽤户需求、提⾼⽤户体验。通常情况下，可以通过多轮会话的上下⽂来收集这些信息，但是基于上下⽂轮数限制，个性化信息⽆法⻓期记忆和保存，此时可以开启⻓期记忆功能，记录并调⽤⽤户的个性化信息。在⼯作流中，也可以通过⻓期记忆节⻓期记忆节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

28/76

点调⽤智能体的⻓期记忆，查询智能体已记录的⽤户喜好、⽤户画像等信息，让⼯作流的效果更加个性化。👉 ⻓期记忆节点需要召回智能体存储的⻓期记忆数据，所以试运⾏⻓期记忆节点或包含⻓期记忆节点的⼯作流时，需要指定⼀个已开启 ⻓期记忆功能的智能体。配置⻓期记忆节点配置说明输⼊参数输⼊参数固定为 Query，表示需要从⻓期记忆中匹配的关键信息，例如查询⽤户的喜好等信息。以新闻搜索的⼯作流为例，Query 可以固定为“喜欢什么类型的新闻”，基于⽤户喜好检索并推荐新闻。Query 可指定为 Reference 或 Input：引⽤：引⽤上游节点的输出参数。输⼊：指定为某个字符串。输出参数输出参数固定为 outputList，格式为 Array，智能体会列举和 Query 相关的⻓期记忆。如果下游节点引⽤了这个参数，智能体会总结⻓期记忆中的内容，并将总结内容作为下游节点的输⼊。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

29/76

调试⻓期记忆节点 调试⻓期记忆节点时需要选择⼀个已开启⻓期记忆能⼒的智能体，如果选择的智能体未开启⻓期记忆则不⽀持调试。使⽤已有的对话类型单智能体来编排业务流程。智能体2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

30/76

配置项说明模型必填下拉选择做智能体输⼊参数必填输⼊需要从知识库中匹配的关键信息参数名默认sys_user_query和sys_user_file_list参数值必填下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

31/76

对话设置智能体如果有设置变量，此处可设置变量值输出值输出该智能体节点输出内容⽀持操作复制：复制当前节点及表单内容删除：删除节点单节点调试⽤于⽤户输⼊的意图识别,根据预设意图选项匹配到不同的分⽀执⾏逻辑，最多 50 个意图，并输出意图分类和分类原因。1. 节点说明 意图识别指的是让智能体理解⽤户通过⾃然语⾔表达的意图或⽬的。⼯作流⽀持意图识别节点对⽤户意图进⾏归类，⽆需再通过⼤模型节点配合选择器节点实现意图识别，使⼯作流运⾏更加⾼效。 意图识别节点可⽤于以下场景：客户服务：识别⽤户问题的类型，并转交各类知识库处理，对于知识库中未匹配的问题，转交⼈⼯客服处理。医疗咨询：对⽤户咨询的医学问题进⾏归类，⾮医学问题的咨询则拒绝回复。综合类智能体：对于功能多样的智能体，可以先由意图识别节点对⽤户咨询进⾏初步分类，转交各个 Agent 分⽀处理。意图识别2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

32/76

配置项说明模型必填下拉选择做意图识别的空间已授权的⼤模型模型参数预设模版HiAgent预置以下三种模型参数模版，提供快捷的模型参数配置。创意模版平衡模版精确模版随机性Temperature采样温度，控制输出的随机性，值越⼤会使输出更随机，更具创造性；值越⼩，输出会更加稳定或确定。默认0.7可修改范围：【0，1】之间，⼩位数⼀位2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

33/76

核采样top_p控制输出的多样性,值越⼤输出包括更多单词选项；值越⼩，输出内容更集中在⾼概率单词上，即输出更确定但缺少多样性。⼀般temperature和top_p只设置⼀个。单次回复max_tokens单次输出内容最⼤token数。当前时间开启后，⽤户的每次 query 中都会拼上当前准确时间。深度思考模式仅针对⽀持深度思考且⽀持深度思考开关的模型⽣效，不可⼿动配置。⻓思考过程展示仅针对⽀持深度思考的模型⽣效，按需配置。当深度思考模式选择"关闭"时，不显示"⻓思考过程展示"选项。输⼊参数必填输⼊做意图分类的输⼊信息参数名默认query⽀持添加/删除⾃定义参数参数值必填下拉选项，可选项包含： - 引⽤：引⽤上游节点的输出参数 - 输⼊：⽀持设定⾃定义的参数值2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

34/76

意图名称必填，⻓度不⼤于30意图匹配后定义的意图名称意图描述必填，⻓度不⼤于1000意图匹配的说明，辅助⼤模型做意图判断，与意图名称成对存在每增加⼀堆意图名称和意图描述节点会⾃动增加下游连接点，⾄多⽀持添加50对，即⾃定义分类最多50类⾼级设置输⼊额外的指令以帮助⼤模型更好的做意图识别,输⼊内容将会被追加到系统提示词中。⽀持引⽤变量：节点输⼊参数。输出值classificationldclassification Namereason超时设置（秒）通过配置超时设置，可以避免⻓时间等待，提⾼系统的响应速度。默认120，取值范围1～600，单位为秒。重试次数重试次数设置允许在请求失败时⾃动重试，提⾼请求的成功率。默认0，取值范围0～3，0表示不重试。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

35/76

⽀持操作复制：复制当前节点及表单内容删除：删除节点单节点调试配置完成后，将意图识别节点与其他节点连接，形成完整的调⽤链路。意图识别节点的每个意图分类，都需要与后续的处理节点相连接，否则意图命中次分类时⽆法触发后续的处理流程。例如在客服智能体中，产品咨询的分类可以流转⾄产品咨询知识库节点处理。推荐为意图识别节点设置兜底策略，若意图未匹配到此处定义的任何分类，则流转到兜底策略处理。循环是⼀种常⻅的控制机制，⽤于重复执⾏⼀系列任务，直到满⾜某个条件为⽌。⼯作流提供循环节点，当需要重复执⾏⼀些操作，或循环处理⼀组数据时，可以使⽤循环节点实现。循环类型 循环节点的配置⽅式取决于循环类型。循环类型是循环节点的运⾏模式，⽀持设置为使⽤数组循环和⽆限循环。 使⽤数组循环 使⽤数组循环类似编程语⾔中的 for 语法结构。遍历循环⽤于遍历⼀个已知的序列，对序列中的每个元素执⾏⼀系列相同的步骤。遍历循环的典型场景如下：⻓⽂⽣成或⻓⽂总结：输⼊每个段落的主题，通过循环节点依次⽣成各个段落，将完整的⽂章打包输出。也可以通过消息节点实现每轮循环之后流式输出段落内容。⻓⽂循环节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

36/76

总结的原理类似。问卷调查：针对多个产品进⾏满意度、NPS 等问卷调查，每个产品的问卷题⽬相同，根据每⼀轮问题的⽤户评分算出各个产品的 NPS 得分。 在编程中，数组是⼀种数据结构，⽤于存储⼀系列元素。数组中的基础概念如下：item：元素，即数组中的单个数据。数组由多个元素组成，每个元素可以是数字、字符、字符串等数据类型。index：索引，指数组中元素的位置。索引从 0 开始计数，表示第⼀次循环，1 表示第⼆次循环，2 表示第三次循环，以此类推。最后⼀次循环的索引为 n-1，其中 n 为循环次数。数组⻓度：数组中元素的数量。 例如，在数组 arrayOfNumbers = [10, 20, 30, 40,50] 中：arrayOfNumbers[0] 的 item 是 10，index 是 0。arrayOfNumbers[1] 的 item 是 20，index 是 1。数组⻓度为 5。 使⽤数组循环时，需要指定循环数组，此参数仅⽀持引⽤上游节点的输出，且必须为数组格式。使⽤数组循环模式下执⾏循环节点时，循环的次数取决于循环数组引⽤的数组⻓度。例如数组 [1,2,3,4,5] 的⻓度为 5，循环节点会循环 5 次完成遍历。如果循环数组参数引⽤了多个数组，则将最短的数组作为循环次数。1. 配置循环节点👉 - 多轮循环之间是严格串⾏执⾏的，不⽀持并发执⾏循环。每个循环迭代完成后，才能启动下⼀个循环迭代。如需并⾏执⾏某些节点，可以使⽤批处理。不⽀持嵌套循环，循环节点中不允许添加另⼀个循环节点，但你可以在循环节点中嵌套⼀个包含循环节点的⼯作流。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

37/76

设置中间变量 循环节点⽀持设置中间变量，此变量可作⽤于每⼀次循环。中间变量通常和循环体中的设置变量节点搭配使⽤，在每次循环结束后为中间变量设置⼀个新的值，并在下次循环中使⽤新值。 例如在⻓⽂⽣成场景中，通过中间变量将每⼀轮的段落总结作为变量传递到下次循环中，让⼤模型参考之前的段落内容和下⼀段的主题，⽣成⼀个上下⽂衔接更流畅的段落。👉 在设置变量节点中，中间变量的数据类型必须和设置值的数据类型⼀致，否则⽆法赋值。设置循环体 创建循环节点后，会⽣成⼀个循环节点和对应的循环体画布。循环体画布是循环节点的内部运⾏机制，⽤于编排循环的主逻辑，每个循环迭代中，⼯作流会依次执2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

38/76

⾏画布内的各个节点。你需要在循环节点和上下游节点之间添加连线，但⽆需调整循环体和循环节点之间的连线。 选中循环体时，才能向循环体中添加新节点，或拖⼊新节点⾄循环体画布。循环体中⽆需设置开始节点或结束节点，默认按照连接线的箭头⽅向依次执⾏各个节点。👉 - 设停⽌循环节点只能在循环体中使⽤。不⽀持将循环体外部的节点拖动⾄循环体内，循环体中的节点也不可移动到循环体之外。调试循环节点 配置循环节点之后，你还需要试运⾏这个节点，查看其输⼊输出是否符合预期。调试结束后，循环节点的运⾏结果中会显示循环节点在多轮循环之后汇总的输⼊输出内容。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

39/76

终⽌循环 ⽤于⽴即终⽌当前所在的循环，跳出循环体。 示例：开发⼀个⽂档内容扩写的⼯作流 ⽤户上传⼀个⽂档⼤纲，通过插件解析⽂档内容，code 节点将段落内容转换成数组，结合循环节点实现每个段落内容的扩写。循环体节点只需要两个节点，⼀个是扩写段落的⼤模型，⼀个是⽤户输出过程中扩写段落的消息节点，⽤来缓解⽤户焦虑，配置如下：⼯作流核⼼节点如下：节点类型说明示例2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

40/76

开始节点定义输⼊参数：input：扩写的⽂档内容插件节点添加⼀个插件，⽤来提取⽂档内容使⽤browser插件进⾏内容提取input: 引⽤start节点上传的⽂档链接code节点使⽤code节点将插件提取的⽂档内容转换成数组，提取⾮空段落内容input：引⽤插件提取的内容content2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

41/76

循环节点设置⼊参是引⽤code节点提取的段落数组；输出扩写的每个段落内容2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

42/76

循环体添加⼀个⼤模型节点，其设置如下：模型：选择⼀个LLM⽂本模型。输⼊： - input参数：引⽤批处理节点的input参数元素（itemin input），表示每次执⾏⼀个数组元素，即段落内容。⽤户提示词：直接引⽤输⼊参数即可，即根据主题 {{input}}扩写成200字⽂章。消息节点：将过程中扩写完成的内容输出，缓解⽤户焦虑2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

43/76

效果查看将上述⼯作流调试、发布后，创建⼀个对话型智能体，直接引⽤这个⼯作流批处理节点⽤于批量执⾏部分操作。 节点说明 ⼯作流执⾏时，每个节点按顺序运⾏⼀次，如果需要⼀次性运⾏多次，可以使⽤批处理节点提⾼⼯作流效率。批处理节点会批量运⾏除批处理和循环节点以外的任何节点，直到达到次数限制或者列表的最⼤⻓度。批处理节点适⽤于⼤量数据并⾏处理的场景。相对于添加多个相同的节点执⾏任务，批处理节点的效率更⾼。例如批量⽣成根据故事⽣成绘本的场景，需要根据每个故事⽚段⽣成对应的图⽚，你可以使⽤批处理节点执⾏⽂⽣图的⼯作流，⼀次性⽣成多张图⽚。👉 - 每⼀批任务默认并⾏运⾏，如需串⾏执⾏，可以将并⾏数量设置为 1，或者使⽤循环节点。批处理节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

44/76

批处理节点中不⽀持添加另⼀个批处理节点或循环节点。在⼯作流中，同⼀时刻只能运⾏⼀个消息节点。请勿通过批处理节点批量并⾏运⾏消息节点，否则可能导致这些节点⽆法按预期正常输出消息。配置批处理节点 批处理节点的处理对象为输⼊参数引⽤的数组结构，例如上游某个节点输出的多条数据结果。批处理节点会遍历数组中的每个元素，对所有元素同时执⾏⼀次性处理，直到所有元素处理完成，或达到指定的次数上限。 批处理设置 为避免并⾏运⾏数量过⼤导致性能⻛险，批处理节点是分批运⾏的，默认每批执⾏ 10 次，最多运⾏200 次。通过批处理设置，你可以调整每⼀批运⾏的最⼤次数，和多批处理的总次数上限。具体参数包括：并⾏运⾏数量上限：每⼀批运⾏的最⼤次数。默认并⾏运⾏ 10 次。此参数可指定为某个固定值，例如 5；也⽀持引⽤上游节点数值类型的的输出参数。NOTE 并⾏运⾏数量参数的引⽤值如果⼤于 10，则设置为 10，如果⼩于 1，则设置为1。批处理次数上限：批处理执⾏总次数达到此上限时，此节点终⽌运⾏。默认批处理次数上限为 100，最⼤⽀持设置为 200。此节点的执⾏逻辑是处理数组中的元素，当批处理次数达到设置的上限时，即使节点未遍历数组中的每个元素，也会停⽌运⾏。 例如批处理的处理对象是⼀个⻓度为 50 的数组，并⾏运⾏数量上限设置为 10，批处理次数上限为 30。那么批处理节点会执⾏ 3 批，每⼀批同时运⾏ 10 次，共计执⾏ 30次后结束运⾏。 输⼊ 输⼊参数指批处理体中要使⽤的参数。批处理节点通常需要批量处理数组中的所有元素，那么可以在批处理节点定义输⼊参数，引⽤前置节点Array 类型的输出，批处理体中就可以引⽤批处理节点输⼊参数中的某个元素或索引，否则只能引⽤上游节点 Array 类型参数的整个数组结构。 批处理节点应最少设置⼀个输⼊参数，且只能引⽤上游节点的 Array 类型。 输出 输出参数是批处理完成后输出的内容，仅⽀持引⽤批处理体中节点的输出变量，格式为数组。 批处理体 批处理体和循环体类似，每个批处理节点都会⽣成⼀个批处理体画布。开发者需要选中批处理体，通过拖拉拽的⽅式将待批处理的节点添加到批处理体中，并使⽤连接线依次将批处理体和各个节点顺序连接起来。你还需要在批处理节点和上下游节点之间添加连线，但⽆需调整批处理体和批处理节点之间的连线。NOTE 不⽀持将批处理体外部的节点拖动⾄批处理体内，批处理体中的节点也不可移动到批处理体之外。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

45/76

示例 例如通过⼤模型进⾏批量主题的内容扩写。添加⼀个批处理节点，其中只有⼀个⼤模型节点，⽤于批量扩写主题。开始节点接收主题数组后传递给批处理节点，⼤模型会处理主题数组中每个主题元素。⼯作流的编排⽅式如下：⼯作流核⼼节点如下：节点类型说明示例开始节点定义输⼊参数：input：数组格式，需扩写的主题。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

46/76

批处理节点设置输⼊参数 topic，引⽤开始节点的 input参数，便于批处理体引⽤。批处理体添加⼀个⼤模型节点，其设置如下：模型：选择⼀个LLM⽂本模型。输⼊： - input参数：引⽤批处理节点的input参数元素（itemin input），表示每次执⾏⼀个数组元素，即主题。⽤户提示词：直接引⽤输⼊参数即可，即根据主题 {{input}}扩写成50字⽂章。连接多个下游分⽀，根据设定的条件按照顺序查找的⽅式来匹配运⾏的分⽀，如果匹配到某条件则只运⾏该条件对应的分⽀，否则继续匹配下⼀条件直⾄结束选择器2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

47/76

配置项说明选择器节点名称必填默认选择器xx序列号作为命名，⽀持重命名所有条件IF和Eles的条件，每个条件对应⼀个流程节点可添加多个Eles if条件表单IFIF条件，包含条件、引⽤变量、选择条件、⽐较值等信息配置条件必填下拉选项，可选项包含：AND（默认选项）、OR2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

48/76

引⽤变量必填引⽤前⾯节点的参数值选择条件必填下拉选项，选项包含：等于（默认选项）、不等于、为空、不为空、包含、不包含引⽤不同参数可⽀持的选项不同⽐较值必填若当前引⽤变量和⽐较值的变量条件命中，则符合当前逻辑判断下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值Else若不满⾜IF的条件，则执⾏该Else链接的下⼀流程⽀持操作复制：复制当前选择器及表单内容删除：删除选择器节点⼯作流变量聚合节点能够将多路分⽀的输出变量整合为⼀个，⽅便下游节点统⼀配置。节点说明 如果⼯作流中设计了多个分⽀，那么往往需要⼀个节点来汇总所有分⽀的输出结果，作为⼯作流的最终输出。如果有任意⼀个分⽀未运⾏，汇总输出的节点会从该分⽀读取到⼀个空值，从⽽导致⼯作流运⾏报错。在这种场景下，你可以使⽤变量聚合节点聚合多路分⽀的输出变量，变量聚合节点会读取多路分⽀中第⼀个不为空的值，供流程下游的节点使⽤和操作，不⽤额外处理未运⾏分⽀的输出结果，简化了数据流的管变量聚合节点2026/1/30 22:06

⼯作流 - HiAgent Document

⽂档中⼼

搜索

⌘ K

简体中⽂

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

49/76

理。 例如选择器或意图识别节点会将⼯作流拆分为多路分⽀，每次执⾏时，⼯作流会根据选择器条件或⽤户意图决定运⾏其中某⼀个分⽀，此时未执⾏的其他分⽀输出变量为空。你可以将多路分⽀都连接到变量聚合节点，并设置⼀个输出变量。⽆论哪条分⽀被执⾏，其结果都能通过这个变量被引⽤与访问，避免了下游节点对相同语义输出变量的重复定义。 变量聚合节点⽀持分组聚合，每组变量会聚合为⼀个输出变量，分组组内的数据类型必须相同。 配置变量聚合节点 配置变量聚合节点时，需要选择聚合策略和聚合变量，并按需设置聚合分组。 聚合策略 通过指定策略对每个分组中的所有变量进⾏聚合处理，同⼀组内的变量实施相应聚合策略。 ⽬前聚合策略仅⽀持设置为“返回每个分组中第⼀个⾮空的值”，你可以拖动变量、调整变量位置。例如组内按顺序设置三个变量output1、output2 和 output3，将其聚合为⼀个变量 Group1，如果 ouput1 不为空，则⽤ output1 的值为 Group1 赋值；如果 ouput1 为空，则取 ouput2 的值，依次类推。新⼿⼊⻔创建会议助⼿新⼿⼊⻔创建会议助⼿2026/1/30 22:06

⼯作流 - HiAgent Document

ON THIS PAGE

⼯作流概述

创建⼯作流

配置⼯作流

Start 节点

End 节点

⼤模型

代码

知识库检索

知识库写⼊

问答库

术语库

⻓期记忆节点

智能体

意图识别

循环节点

批处理节点

选择器

变量聚合节点

插件

消息节点

⼯作流

数据库

HTTP 节点

信息收集节点

图像⽣成节点

⼯作流画布操作

⼯作流搭建示例

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

50/76

聚合变量 在 Group 1 中选择需要聚合的变量，分组内变量的数据类型必须相同。变量聚合节点⽀持聚合多种数据类型，包括字符串（String）、数字（Integer、Number）、⽂件（File）对象（Object）以及数组（Array）等。每个分组只能聚合⼀种数据类型的变量。例如将多个 String 类型的变量聚合为⼀个String 变量、将多个 Integer 类型的变量聚合为⼀个 Integer 变量。 聚合分组 默认只有⼀个分组 Group1，对应⼀个输出变量 Group1。Group1 分组中所有变量类型和输出的变量类型相同。如果需要输出多个变量，可以添加多个分组。例如每个分⽀都有搭建智能体团队空间智能体插件知识库数据库提示词模板模型管理账号中⼼审批中⼼平台管理（管理员）创建会议助⼿⼯作流模型⼴场模型体验⼤语⾔模型体验访问控制资源管理⽤户管理⻆⾊管理组织管理模型管理空间管理智能体管理搭建智能体团队空间智能体插件知识库数据库提示词模板模型管理账号中⼼审批中⼼平台管理（管理员）创建会议助⼿⼯作流模型⼴场模型体验⼤语⾔模型体验访问控制资源管理⽤户管理⻆⾊管理组织管理模型管理空间管理智能体管理2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

51/76

两个输出变量 String 和 Integer，可以设置两个分组，分别⽤于聚合 String 和Integer。输出 变量聚合节点的输出变量固定为 Group1，是分组 Group1 聚合的返回结果，具体的值取决于聚合策略。如果有多个分组则根据分组数量递增为 Group2、Group3等变量。输出变量的数据类型取决于对应分组聚合的变量数据类型。 示例 在客服类应⽤中，通过 IF 选择器判断⽤户输⼊的问题属于哪个分类，并将对应的问题流转给对应的分⽀处理，例如产品问题流转给产品推荐的⼤模型回答问题。三个分⽀均汇总到变量聚合节点，汇总为两个参数，即问题类型（type）、回复内容（answer），并传递到结束节点回答⽤户问题。插件2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

52/76

通过插件可以扩展智能体的能⼒，使智能体能与外部环境交互，通过连接其他的系统或服务完成复杂的任务。 ⽀持 OAuth2 鉴权的⻜书插件，⽀持在节点试运⾏时设置授权，授权后继续执⾏。配置项说明插件节点名称必填默认插件xx序列号作为命名，⽀持重命名插件点击后，跳出插件选择弹窗，可选择插件中⼼或当前空间内已创建的插件使⽤，扩展功能输⼊参数所选插件⼯具的输⼊参数参数名所选插件⼯具的参数名参数值必填2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

53/76

下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值输出值所选插件⼯具的输出参数异常忽略开启后，⽀持配置异常忽略模板，发⽣异常后，⼯作流运⾏不会中断。开发者可⾃定义设置需要返回的输出字段内容，必须是输出中已定义的字段，且格式为合法的 JSON 格式。另外，节点还会返回输出参数errorBody，传递节点异常的详细信息。若未开启，则节点执⾏异常后会触发⼯作流的终⽌运⾏。⽀持操作复制：复制当前插件及表单内容删除：删除插件节点通常情况下，⼯作流会在执⾏完毕后通过结束节点输出最终的执⾏结果。当⼯作流处理流程较⻓、运⾏时间较久时，开发者可以在⼯作流中添加输出节点，临时输出⼀段消息，避免⽤户等待时间过⻓、放弃对话。例如提示⽤户任务正在执⾏中，建议⽤户耐⼼等待。 在消息节点中，⼯作流会在执⾏的同时输出⼀条中间消息，消息内容为指定的输出内容，其中可引⽤已定义的输出变量。消息节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

54/76

配置项说明消息节点名称必填默认消息xx序列号作为命名，⽀持重命名2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

55/76

输出参数必填输出变量⽤于定义输出内容中可引⽤的变量，⽀持添加多个变量。每个变量均需要设置参数名和参数值，其中参数值可指定为某个固定值，或引⽤上游节点的输出变量。回答内容必填在⼯作流运⾏过程中，智能体将直接使⽤这⾥指定的内容回复对话。⽀持使⽤{{变量名}}的⽅式引⽤输出变量中定义的变量。回答效果配置流式输出：默认关闭。开启后，回复内容中的⼤语⾔模型的⽣成内容将会逐字流式输出。 > 👉 流式输出约束：1.回答内容⾥要引⽤变量，且变量引⽤的是LLM节点类的输出参数，包括LLM节点（要求LLM节点的输出模式是⾮JSON类型）、agent节点、⼯作流节点（end节点是流式⽣效）推理渲染：默认关闭。开启后，消息输出内容将会被渲染成类深度推理过程的效果。并⼊会话历史：默认开启，开启后，智对话历史轮次消息输出内容将作为当前对话上下⽂。⽀持操作复制：复制当前节点及表单内容删除：删除节点⽀持单节点调试通过引⽤⼯作流类型节点实现将复杂的任务或流程按照⼦功能或者⼦模块能⼒拆分，从⽽使流程细化、便于管理和维护。⼯作流2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

56/76

补充说明：当前⼯作流只⽀持两级，即已经嵌套其他⼯作流的 flow 不再⽀持被嵌套使⽤配置项说明⼯作流节点名称必填默认⼯作流xx序列号作为命名，⽀持重命名⼯作流点击后，可选择当前空间内已发布的⼯作流使⽤输⼊参数所选⼯作流的输⼊参数参数名所选⼯作流的参数名参数值必填2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

57/76

下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值输出值所选⼯作流的输出参数异常忽略开启后，⽀持配置异常忽略模板，发⽣异常后，⼯作流运⾏不会中断。开发者可⾃定义设置需要返回的输出字段内容，必须是输出中已定义的字段，且格式为合法的 JSON 格式。另外，节点还会返回输出参数 errorBody，传递节点异常的详细信息。若未开启，则节点执⾏异常后会触发⼯作流的终⽌运⾏。⽀持操作复制：复制当前⼯作流及表单内容删除：删除⼯作流节点数据库节点⽤于对指定数据库进⾏常⻅的 SQL 操作。1. 节点说明 ⼯作流中⽀持添加数据库节点，数据库节点⽀持智能体⽤户通过 NL2SQL⽅式⾃⾏查询数据库，⽤户的⾃然语⾔可转为 SQL 语句，系统会按照数据库节点中配置的 SQL 语句来执⾏数据表操作。数据库2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

58/76

配置项说明模型必填下拉选择空间已授权的⼤模型输⼊参数必填参数名默认query⽀持新增/删除⾃定义参数参数值必填下拉选项，可选项包含： - 引⽤：引⽤上游节点的输出参数 - 输⼊：⽀持设定⾃定义的参数值2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

59/76

⾼级设置输⼊额外的指令以帮助⼤模型更好的⽣成sql，输⼊内容将会被追加到系统提示词中。⽀持引⽤变量：节点输⼊参数。重试次数重试次数设置允许在请求失败时⾃动重试，提⾼请求的成功率。默认为3，取值范围为0～10，0表示不重试。输出值query_resultsql_statement⽀持操作复制：复制当前节点及表单内容删除：删除当前节点单节点调试HTTP 请求节点允许⽤户通过 HTTP 协议发送请求到外部服务，实现数据的获取、提交和交互。⽀持多种 HTTP 请求⽅法，并允许⽤户配置请求参数、请求头、鉴权信息、请求体等，以满⾜不同的数据交互需求。此外，HTTP 请求节点还提供了超时设置、重试机制，确保请求的可靠性和数据的正确处理。 HTTP 请求节点的作⽤如下：数据获取：从外部服务获取数据，例如从 API 获取⽤户信息、天⽓数据等。数据提交：向外部服务提交数据，例如提交表单数据等。数据更新：更新外部服务中的数据，例如更新⽤户信息、修改订单状态等。数据删除：删除外部服务中的数据，例如删除⽤户账户、删除订单等。 节点配置 在使⽤ HTTP 请求节点实现数据获取、提交等功能时，通常需要配置 API、请求参数、请求头、鉴权、请求体等参数。为了简化这⼀配置过程，提⾼开发效率，HTTP 请求HTTP 节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

60/76

节点⽀持导⼊ cURL 功能，开发⼈员可以快速将 cURL 命令中的参数导⼊到 HTTP 请求节点。配置项说明请求配置配置 API 请求地址和⽅法，⽀持以下请求⽅法：GET：⽤于请求从外部服务获取数据，例如调⽤ API 获取⽤户信息、天⽓数据等。POST：⽤于向服务器提交数据，例如提交表单。HEAD：类似于 GET 请求，但服务器不返回请求的资源主体，只返回响应头。PATCH：⽤于在请求-响应链上的每个节点获取传输路径。PUT：⽤于向服务器上传资源，通常⽤于更新已存在的资源或创建新的资源。DELETE：⽤于请求服务器删除指定的资源。配置API请求地址时，可以使⽤变量来动态指定参数，通过输⼊{{即可唤出并使⽤该变量。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

61/76

Header设置请求头包含客户端的信息，如 User-Agent、Accept 等。通过配置请求头，可以指定客户端的类型、接受的数据格式等信息。Param设置请求参数是附加在 URL 后⾯的键值对，⽤于向服务器传递额外的信息。例如，在搜索请求中，可以通过请求参数传递搜索关键词。鉴权设置通过配置鉴权信息，可以确保请求的安全性，防⽌未授权的访问，⽀持 Bearer Token 和⾃定义鉴权：Bearer Token：输⼊ Token 值，通常⽤于 OAuth2 等认证⽅式。⾃定义鉴权：配置⾃定义的认证信息，包括： - Key：认证键。 - Value：认证值。 - Add To：选择将认证信息添加到请求头（Header）或查询参数（Query）。Body设置请求体是 POST 请求中包含的数据，可以是表单数据、JSON 数据等。根据不同的请求类型和数据格式，可以选择相应的请求体格式，例如 form-data、x-www-form-urlencoded、raw text、JSON 等。超时设置（秒）通过配置超时设置，可以避免⻓时间等待，提⾼系统的响应速度。默认120，取值范围为0-600，单位为秒。重试次数重试次数设置允许在请求失败时⾃动重试，提⾼请求的成功率。默认为3，取值范围为0～10，0表示不重试。输出值输出变量包括响应体、状态码和响应头。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

62/76

异常忽略开启后，⽀持配置异常忽略模板，发⽣异常后，⼯作流运⾏不会中断。开发者可⾃定义设置需要返回的输出字段内容，必须是输出中已定义的字段，且格式为合法的 JSON 格式。另外，节点还会返回输出参数 errorBody，传递节点异常的详细信息。若未开启，则节点执⾏异常后会触发⼯作流的终⽌运⾏。配置示例 例如通过 HTTP 请求节点调⽤ HiAgent 的平台接⼝，实现与智能体对话。通过信息收集节点向⽤户提问，等待⽤户回复，并将⽤户回复及抽取的信息作为输出参数。信息收集节点⽀持以下操作：复制：复制当前节点及表单内容。删除：删除节点。调试：配置输⼊输出参数进⾏对话体验，运⾏结束后，单击展示结果查看运⾏结果。节点未引⽤ file 类型时，直接开始对话；引⽤了 file 类型时，需要先上传⽂件，才能开始对话。信息收集节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

63/76

配置项是否必填说明信息收集节点名称是默认信息收集xx序列号作为命名，⽀持重命名。模型是下拉选择空间已授权的⼤模型，⽀持配置模型参数。输⼊参数否输⼊需要添加到问题的参数。参数名只能包含数字、字⺟、下划线，并且以字⺟或下划线开头，最⻓64个字符。单击+图标⽀持添加多个，单击—图标⽀持删除。提问内容是设置对⽤户发出提问的具体内容，可以使⽤{{变量名}}的⽅式引⼊输⼊参数中的变量。参数抽取否填写待抽取的参数名、类型、描述。参数名只能包含数字、字⺟、下划线，并且以字⺟或下划线开头，最⻓64个字符。描述最⻓1000个字符。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

64/76

单击+图标⽀持添加多个，单击—图标⽀持删除。输出值是默认输出参数为USER_RESPONSE，如果配置了参数抽取，那么也会包含所配置的参数名。图像⽣成节点通过⽂字描述⽣成图⽚。图像⽣成节点⽀持以下操作：复制：复制当前节点及表单内容。删除：删除节点。调试：配置输⼊输出参数进⾏对话体验，运⾏结束后，单击展示结果查看运⾏结果。节点未引⽤ file 类型时，直接开始对话；引⽤了 file 类型时，需要先上传⽂件，才能开始对话。配置项说明图像⽣成节点名称必填默认图像⽣成xx序列号作为命名，⽀持重命名图像⽣成节点2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

65/76

模型必填下拉选项，选择⾼级能⼒⽀持⽂⽣图的视觉模型。⽐例必填提供以下图⽚⽐例： - 16:9（1280x720 ） - 3:2（1248x832） - 4:3（1152x864） - 1:1（1024x1024 ） -3:4（864x1152） - 2:3（832x1248 ） - 9:16（720x1280 ）- 21:9（1512x648） - ⾃由：⼿动输⼊，⽀持的宽⾼范围为[512,2048]输⼊参数必填⽀持多组参数，可⼿动增加、删除参数名 - 定义输⼊的参数名，输⼊的变量可以被下⽅的提示词调⽤ - 只能包含数字、字⺟、中划线或下划线 - 字符限制：【1，64】字符 - 不可与已有参数名称重复参数值 - 下拉选项，可选项包含： - 引⽤：引⽤前⾯节点的参数值 - 输⼊：⽀持设定⾃定义的参数值提示词必填视觉模型的提示词，⽤于指定⽣成内容。⻓度1～2000字符。编写提示词时，可以引⽤输⼊参数中的变量。⽂本权重⽂本权重（scale），表示⽂字（创意描述）对结果图像的影响程度，数值越⼤图像越接近⽂本，数值越⼩图像可能更具创造性。默认是2.5，取值范围为1-10，精细度0.1。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

66/76

超时设置（秒）通过配置超时设置，可以避免⻓时间等待，提⾼系统的响应速度。默认为120，取值范围1～600，单位为秒。重试次数重试次数设置允许在请求失败时⾃动重试，提⾼请求的成功率。默认为3，取值范围0～10，0表示不重试。输出值输出变量默认是：dataMsg开启异常处理后，增加输出以下内容：errorBody- Message - Code异常处理开启后，⽀持配置异常忽略模板，发⽣异常后，⼯作流运⾏不会中断。开发者可⾃定义设置需要返回的输出字段内容，必须是输出中已定义的字段，且格式为合法的 JSON 格式。另外，节点还会返回输出参数 errorBody，传递节点异常的详细信息。若未开启，则节点执⾏异常后会触发⼯作流的终⽌运⾏。连线：光标从上级节点右侧端点开始，往下级节点左侧端点画线。 ⿏标悬停在两个节点中间的连线上，单击 + 图标，⽀持添加新节点。删除连线：选中连线，Windows 系统按 Backspace、Delete 键，MacOS 系统按Delete 键。⼯作流画布操作2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

67/76

优化布局：在画布左下⻆单击优化布局按钮，⼀键优化⼯作流布局。 优化前：优化后：注释：单击添加注释，在画布⾥会出现注释框，⽤户可对画布编排逻辑添加注释内容，注释框位置可拖动。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

68/76

试运⾏：配置完成的⼯作流，需要先进⾏试运⾏，试运⾏通过后才可发布。发布后，智能体可添加使⽤。 试运⾏结束后，窗⼝会显示本次运⾏结果。运⾏历史：显示⼯作流的全部运⾏历史，单击列表中的运⾏时间，⽀持查看详细运⾏耗时。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

69/76

复制：单击复制按钮，可复制当前的⼯作流快速使⽤。 填写复制后的⼯作流名称，即可完成复制⼯作流操作。隐藏/展示上⼀次运⾏结果展示上⼀次运⾏结果：查看历史试运⾏记录、调⽤树节点输⼊输出，并⽀持节点定位。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

70/76

隐藏上⼀次运⾏结果：以搭建⼀个产品售后机器⼈为例，演示如何配置⼯作流模式智能体的专属⼯作流。⼯作流搭建示例2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

71/76

产品售后机器⼈主要⽤于售后场景，提供基础的问答服务与售后问题过滤的能⼒。在这个⼯作流中，⾸先通过⼤模型节点对问题进⾏分类，并通过选择器节点将不同类型的问题流转到不同的分⽀处理，最终在结束节点引⽤各个分⽀的输出。详细配置说明如下：环节节点类型说明示例开始开始节点开始节点⽤于接收⽤户问题，并透传给后续的节点。识别⽤户意图⼤模型节点本示例中，⼤模型节点⽤于识别进线⽤户的意图，判断⽤户问题的分类。2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

72/76

分发⽤户问题选择器节点选择器节点⽤于将问题进⾏分发，转交给不同的分⽀进⾏处理，例如将产品咨询问题转交给咨询类知识库处理，故障排查类问题转交给故障排查知识库处理。处理⽤户问题知识库节点、⼤模型节点产品咨询分⽀和故障排查分⽀均使⽤此逻辑。处理流程如下：1. 知识库节点匹配⽤户问题。1. ⼤模型节点包装回复。如果未匹配到知识库，则返回兜底⽂案。结束结束节点根据前置步骤⽣成的最终答案，输出智能体的回复内容。你可以通过输出变量直接引⽤前置节点的返回数据。⽀持对⼯作流的编辑、删除、导出⼯作流、导⼊⼯作流操作⽀持按发布状态（已发布、未发布）筛选，⼯作流名称模糊查询管理⼯作流2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

73/76

删除⼯作流： 点击删除 icon，填写当前⼯作流名称，即可删除。删除后，引⽤该⼯作流的智能体/⼯作流，将失效该⼯作流，请前往引⽤的智能体/⼯作流删除或重新添加其他⼯作流导⼊⼯作流： 可本地上传 Json 格式的⽂件，填写⼯作流名称后即可导⼊成功2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

74/76

管理⼯作流版本 ⼯作流⽀持通过版本的⽅式记录⼯作流的编辑和发布历史，你可以查看⼯作流的每⼀个发布版本，或者回退到指定版本。 在⼯作流编辑⻚⾯中单击查看历史图标，可以查看并管理⼯作流的所有历史版本。 ⽣成⼯作流版本 发布⼯作流时，你需要设置⼯作流的版本号和版本描述，这些信息会记录在⼯作流历史版本⻚⾯，便于后期追溯和参考。查看⼯作流历史版本 在⼯作流的编排⻚⾯右上⻆单击发布历史图标，可以查看⼯作流的版本列表。此⻚⾯根据操作时间倒序展示当前⼯作流所有提交和发布的历史记录，2026/1/30 22:06

⼯作流 - HiAgent Document

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

75/76

包括版本号、版本描述、发布时间。你可以你可以查看或试运⾏某个版本的⼯作流，但⽆法编辑。 在历史⻚⾯单击指定的版本记录，会在当前⻚⾯⾃动加载这个版本的编排详情在历史版本⻚⾯，可查看指定历史版本后，点击“还原为此版本”，此⼯作流版本会覆盖当前你的草稿版本。 如果你希望将线上版本回退到某个历史版本，需要先加载这个历史版本到草稿，再发布⼯作流。这个新的版本也会作为⼀条新的发布历史记录在版本历史中。2026/1/30 22:06

⼯作流 - HiAgent Document

上⼀⻚

插件

hiagent.deyunai.com:32300/platform/doc/user-guide/workﬂow

76/76

👉 - 引⽤⼯作流或升级⼦⼯作流版本时，不⽀持选择某个历史版本，必须使⽤最新版本。升级到最新版本后，当前⼯作流中所有使⽤此资源的节点均会同步升级。引⽤⼯作流或升级⼦⼯作流版本时，不⽀持选择某个历史版本，必须使⽤最新版本。升级到最新版本后，当前⼯作流中所有使⽤此资源的节点均会同步升级。